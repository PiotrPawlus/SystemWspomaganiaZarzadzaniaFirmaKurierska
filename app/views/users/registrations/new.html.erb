        <div class="wrapper-login">
            <div class="container">
                <div class="row">
                    <div class="col-xs-12 text-center">
                        <h3>Zarejestruj użytkownika</h3>
                    </div>
                    <div class="col-xs-6 col-xs-offset-3 text-center panel-outer">
                        <div class="panel">
                          <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
                            <%= devise_error_messages! %>

                            <div class="form-group is-empty">
                                <label for="inputImie" class="col-md-2 control-label">Email</label>
                                <div class="col-md-10">
                                  <%= f.email_field :email, autofocus: true, class: "form-control", placeholder: "Email" %>
                                </div>
                            </div>

                            <div class="form-group is-empty">
                                <label for="inputNazwisko" class="col-md-2 control-label">Password</label>
                                <div class="col-md-10">
                                 <% if @minimum_password_length %>
                                  <% end %>
                                  <%= f.password_field :password, autocomplete: "off",class: "form-control", placeholder: "Password" %>
                                </div>
                            </div>

                            <div class="form-group is-empty">
                                <label for="inputTelefon" class="col-md-2 control-label">Password</label>
                                <div class="col-md-10" style="text-align: left; margin-top: 10px;">
                                    <%= f.password_field :password_confirmation, autocomplete: "off", class: "form-control", placeholder: "Password confirmation" %>
                                </div>
                            </div>

                            <%= render(:partial => "form", :locals => {:f => f})%>

                            <div class="actions">
                              <%= f.submit "Sign up" %>
                            </div>
                          <% end %>

                          <%= render "devise/shared/links" %>
                        </div>
                    </div>
                </div>
            </div>
        </div>