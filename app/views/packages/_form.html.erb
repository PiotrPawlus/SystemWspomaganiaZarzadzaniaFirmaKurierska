<table>
  <tr>
    <th> <%= field.label(:weight, "Waga:")%> </th>
    <td> <%= field.email_field(:weight)%></td>
  </tr>
  <tr>
    <th> <%= field.label(:size_X, "Długość:")%> </th>
    <td> <%= field.text_field(:size_X)%></td>
  </tr>
  <tr>
    <th> <%= field.label(:size_Y, "Szerokość:")%> </th>
    <td> <%= field.text_field(:size_Y)%></td>
  </tr>
  <tr>
    <th> <%= field.label(:size_Z, "Wysokość:")%> </th>
    <td> <%= field.text_field(:size_Z)%></td>
  </tr>
  <tr>
    <th> <%= field.label(:delivery_address, "Adres dostawy:")%> </th>
    <td> <%= field.text_field(:delivery_address)%></td>
  </tr>
  <tr>
    <th> <%= field.label(:sender_address, "Adres wysyłającego:")%> </th>
    <td> <%= field.text_field(:sender_address)%></td>
  </tr>
  <tr>
    <th> <%= field.label(:actual_place, "Aktualny miejsce paczki:")%> </th>
    <td> <%= field.text_field(:actual_place)%></td>
  </tr>
  <tr>
    <th> <%= field.label(:phone, "Telefon: ")%> </th>
    <td> <%= field.text_field(:phone)%></td>
  </tr>
  <tr>
    <th> <%= field.label(:cost, "Koszt: ")%> </th>
    <td> <%= field.text_field(:cost)%></td>
  </tr>

</table>
